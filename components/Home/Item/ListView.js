import React from 'react';
import { View, Text } from 'react-native';

/**
 * Component for list view of an things to do in that location.
 *
 * @class ListView
 * @extends {React.Component}
 */
class ListView extends React.Component {
  /**
   * Renders JSX element.
   *
   * @returns
   * @memberof ListView
   */
  render() {
    return (
      <View>
        <Text>ListView</Text>
      </View>
    );
  }
}

export default ListView;
