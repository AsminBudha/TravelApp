import React from 'react';
import { Text } from 'react-native';

/**
 * List of favourite location in home.
 *
 * @class FavList
 * @extends {React.Component}
 */
class FavList extends React.Component {
  /**
   * Renders JSX element.
   *
   * @returns
   * @memberof FavList
   */
  render() {
    return (
      <Text>Hello</Text>
    );
  }
}

export default FavList;
