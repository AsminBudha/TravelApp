import React from 'react';
import { View, Text } from 'react-native';

/**
 * Screen for Profile in home screen tab.
 *
 * @class Profile
 * @extends {React.Component}
 */
class Profile extends React.Component {

  /**
   * Renders JSX element.
   *
   * @returns
   * @memberof Profile
   */
  render() {
    return (
      <View>
        <Text>Profile</Text>
      </View>
    );
  }
}

export default Profile;
